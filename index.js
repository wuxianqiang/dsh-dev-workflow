export const name = 'dsh-dev-workflow'

export function apply() {
  // Skill-only bundle. The workflow is implemented by the packaged Skills
  // and DSH's native agent, filesystem, subagent, and workflow capabilities.
}
